{
	"version": "2.0.0",
	"tasks": [
		{
			"type": "shell",
			"command": "npm run build && npm run lint && npm run test",
			"label": "Build, lint, and test the Next.js app",
			"group": "build",
			"problemMatcher": [
				"$tsc"
			]
		}
	]
}